To install SqlPkg follow these steps:

1. Copy the contents of the "publish" folder to "/usr/bin/sqlpkg/"
2. Execute the install script using:
   sh install.sh

Once complete, quit the terminal and open a new one to begin using SqlPkg.
